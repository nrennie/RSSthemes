#' Complete list of palettes.
#'
#' Use names(RSSPalettes) to return all palette names.
#'
#' @export
RSSPalettes <- list(
  # sequential palettes
  Significance = list(c("red", "lightgrey", "black"),
                      "seq",
                      FALSE))
